#include<stdio.h>

int  main() {
 int num,m,n,i;
 printf("Enter the Range Between m,n ");
 scanf("%d%d", &m,&n);
 for (num = m; num <= n; num++) {
   for(i = 2; i < num; i++) {
    if (num % i == 0)
      break;
   }
   if (i == num)
     printf("%d ", num);
 }
  getch();
}
